<%
  percentage = data.percent_in_favor
  percentage = data.percent_against if data.percent_against > data.percent_in_favor;
  position   = data.in_favor_count >= data.against_count ? "in_favor" : "against"
%>

<%= link_to raw("<strong>#{percentage}<span class='percentage'>%</span></strong><span>#{t("." + position )}</span>"), proposal_path(data.id), :class => "icon #{ position }" %>
<div class="content">

  <p><strong><%= data.title %></strong></p>

  <div class="footer">
    <span class="author">
      <%= raw t('.author', :author => link_to(data.author.fullname, user_path(data.author.id)), :time => distance_of_time_in_words_to_now(data.published_at)) %>
    </span>
    &middot;
    <%= link_to t('shared.lists_elements.comments', :count => data.comments_count), proposal_path(data.id, :anchor => :comments), :class => "participants-count" %>
    &middot;
    <%= t('.votes', :count => data.participation) %>
    &middot;
    <%= render "/shared/inline_sharing" %>
  </div>
  <%= render 'shared/lists_elements/last_comments', :data => data %>
</div>
