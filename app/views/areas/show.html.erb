<%= render 'summary' %>

<%= menu [
  { :link => (link_to t(".menu.summary"), area_path(@area)), :class => :active},
  { :link => (link_to t(".menu.actions"), actions_area_path(@area)) },
  { :link => (link_to t(".menu.questions"), questions_area_path(@area)) },
  { :link => (link_to t(".menu.proposals"), proposals_area_path(@area)) },
  { :link => (link_to t(".menu.agenda"), agenda_area_path(@area)) },
  { :link => (link_to t(".menu.documents"), "#")},
] %>

<%= render 'shared/actions' %>
<%= render 'shared/questions' %>
<%= render 'shared/proposals' %>
<%= render 'shared/agenda', :title => t('.agenda.title') %>
