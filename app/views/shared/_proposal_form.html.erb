<% if user_signed_in? && @proposal.present? %>
  <article id="proposal-123" class="mini popover with_footer initialy_hidden">
  <%= form_for @proposal, :remote => true, :html => {:'data-type' => 'html'} do |form| %>
    <div class="inner">
      <header><h2>Haz una pregunta</h2></header>
      <div class="content">
        <p>Recuerda ser breve y conciso. Así te asegurarás una respuesta en menos tiempo.</p>
        <h3>Tu pregunta</h3>

        <%= form.fields_for :proposal_data, @proposal_data do |proposal_data| %>
          <%= proposal_data.hidden_field :user_id %>
          <%= proposal_data.hidden_field :area_id %>

          <div id="123" class="input-counter">
            <span class="counter">140</span>
            <%= proposal_data.text_area :proposal_text %>
          </div>

        <% end %>

      </div>
      <span class="close"></span>
    </div>
    <footer>
    <div class="separator"></div>
    <div class="inner">
      <%= form.submit t('.make_a_proposal.submit'), :id => 'submit-123', :class => 'disabled white_button right', :disabled => 'disabled' %>
    </div>
    </footer>
    <div class="t"></div><div class="f"></div>
  <% end %>
  </article>
<% end %>
