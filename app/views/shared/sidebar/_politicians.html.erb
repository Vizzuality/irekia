<% if @content.users.size > 0 or @editable %>
  <h4><%= t('.title') %></h4>
  <ul class="people no-border no-padding extra-margin-bottom<%= ' editable' if @editable %>">
    <% @content.users.each do |politician| -%>
      <li<%=" class='editable'" if @editable %>>
      <%= avatar politician, 'small' %>
      <%= link_to politician.fullname, path_for_user(politician), :class => :name %>
      <span class="position"><%= get_politician_title(politician) %></span>
      </li>
    <% end -%>

    <% if @editable %>
      <li class="add">
      <%= link_to "Etiquetar político", "#", :class => :add %>
      <div class="input_field">
        <input type="text" />
      </div>
      <span class="spinner" id="politician_spinner"></span>
      </li>
    <% end %>
  </ul>
<% end %>
