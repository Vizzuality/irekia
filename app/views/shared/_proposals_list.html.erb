<ul>
  <% @proposals.each do |proposal| %>
    <li class="proposal">
    <%= render "shared/lists_elements/#{proposal.class.name.downcase}#{'_mini' if defined?(mini) && proposal.is_a?(Proposal)}", :data => proposal %>
  <% end %>
</ul>
